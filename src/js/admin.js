

function delete() {
    fetch("http://localhost:8080/api/users", {
        method: "DELETE",
        User, err: JSON.stringify({
            title: "delete",
            headers: {
                'Content-Type': 'application/json'
              } 
        }),
        const Json = await response.json() {
            jdoit delete mon js ici i guess
            pute pute pute salope pute
        }
    })
    euitizaebhgia"jrkegeraguibjka"erg
}